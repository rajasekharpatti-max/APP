# Anusha Jewelry — Android / Play Store

Package: `com.anushajewelry.shop`

This wraps the shop book as a phone app (Capacitor). Data stays on the device.

```bash
cd mobile
npm install
npx cap sync android
npx cap open android
```

Publish steps (your Play Console): [docs/PLAY_STORE.md](../docs/PLAY_STORE.md)
